{
  "name": "polymorph",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.22.0",
  "description": "Chrome MV3 extension that transforms posts matching English rules judged by Jev.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build-icons.mjs && tsc --noEmit && vite build && vite build --config vite.content.config.ts",
    "icons": "node scripts/build-icons.mjs",
    "qa": "node scripts/qa/run.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify-dist.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "happy-dom": "^20.14.5",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}